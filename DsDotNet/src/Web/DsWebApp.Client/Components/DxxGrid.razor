@* DxxGrid *@
@inherits Dual.Web.Blazor.Client.Components.Grid.DxxGridBase
@_renderBase()

@code {
    RenderFragment _renderBase() => builder => base.BuildRenderTree(builder);
    protected override async Task<ClientSettings> GetClientSettings() =>
        (ClientSettings)(await DsClientSettings.ReadAsync(LocalStorage));
}

