[sys ip = localhost] double = {
    [flow] Flow = {
        RET <||> ADV;
    }
    [interfaces] = {
        ADV = { Flow.ADV ~ Flow.ADV }
        RET = { Flow.RET ~ Flow.RET }
        RET <||> ADV;
    }
}