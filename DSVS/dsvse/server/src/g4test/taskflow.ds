[sys]MyElevatorSystem = {
    [task]M = {
        U;
        D;
        A12 = { M.U ~ S.S2U }
    }

    [flow of B]remember_call_set = {
        // 호출 Set기억 
        //#g(A), B.X1 > Set1F <| T.A21 ? T.X;
        A, B ? C > D, E;
    }
}