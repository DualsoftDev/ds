[sys]My = {
    // A > B > C > D > E;
    // P |> Q;
    // Q |> R;
    // X <||> Y;

    A, B  > D;

    //(!A) > C;
    // (@xor(A, B) & D|E) > C;
    // @set(AB) > X;

    // A, B |>> C, D;
    // E, F <||> G.X, H;
    // (A & B) > C;
    // A.B, C.D |> E.F, G.H;
    // A1, B1 > C1, D1 < E1, F1;
    // A, B, C > D.e 
}
