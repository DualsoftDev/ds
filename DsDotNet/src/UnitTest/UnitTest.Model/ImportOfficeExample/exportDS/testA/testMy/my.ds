[sys ip = localhost] my = {
    [flow] STN01 = {
        WORK2 => WORK3 => WORK1 => WORK2;
        WORK1 = {
            "Pin_+" > "Pin_-";
        }
    }
    [jobs] = {
        "Pin_+" = { Pin."+"(_, _); }
        "Pin_-" = { Pin."-"(_, _); }
    }
    [device file="DS_Library.ds"] Pin; 
}