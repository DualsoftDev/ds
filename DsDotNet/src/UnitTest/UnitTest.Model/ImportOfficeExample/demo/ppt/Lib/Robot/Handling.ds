[sys] Handling = {
    [flow] Page1 = {
    }
    [flow] Flow = {
            R2; // island
            R1; // island
            R3; // island
    }
    [interfaces] = {
        UNLOAD = { Flow.R3 ~ _ }
        LOAD = { Flow.R2 ~ _ }
        HOME = { Flow.R1 ~ _ }
        LOAD <||> HOME;
        LOAD <||> UNLOAD;
        HOME <||> UNLOAD;
    }
}
