[sys] Double = {
    [flow] Flow = {
        RET <||> ADV;
    }
    [interfaces] = {
        ADV = { Flow.ADV ~ Flow.ADV }
        RET = { Flow.RET ~ Flow.RET }
        RET <||> ADV;
    }
}
//DS Language Version = [0.9.2.2]