namespace Dual.Common

module Operators =

    // https://docs.microsoft.com/en-us/dotnet/fsharp/language-reference/operator-overloading
    //
    // PREFIX operators : "!", "~" 로 시작 (단, "!=" 는 제외)
    // PREFIX or INFIX 둘다 사용 가능 : +, -, +., -., &, &&, %, and %%
    //      단, 이때 정의시에 ~ 를 붙이면 prefix, 없으면 infix.  단, 사용시에는 ~ 떼고 사용

    ()