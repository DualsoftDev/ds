[sys ip = localhost] Sensor = {
    [flow] Page1 = {
    }
    [flow] Flow = {
        PositionRET <||> PositionADV;		// PositionRET(Real)<||> PositionADV(Real);
    }
    [interfaces] = {
        OFF = { _ ~ Flow.PositionRET }
        ON = { _ ~ Flow.PositionADV }
        OFF <||> ON;
    }
}
