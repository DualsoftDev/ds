[sys] Handling = {
    [flow] Flow = {
            R1; 
            R2; 
            R3; 
    }
    [interfaces] = {
        HOME = { Flow.R1 ~ _ }
        LOAD = { Flow.R2 ~ _ }
        UNLOAD = { Flow.R3 ~ _ }
        LOAD <||> HOME;
        LOAD <||> UNLOAD;
        HOME <||> UNLOAD;
    }
}