using IO.Spec;

namespace ThirdParty.AddressInfo.Provider
{
    public class AddressInfoProviderLsXGI : IAddressInfoProvider
    {
        public bool GetAddressInfo(string address, out int byteOffset, out int bitOffset, out int contentBitLength)
        {
            throw new NotImplementedException();
        }
    }
}