

//////////////////////////////////////////////////////
//DTS auto generation MY ExSegs
//////////////////////////////////////////////////////
[sys] EX = {
    [flow] F1_ER1 = { EXT; }
    [flow] F1_ER2 = { EXT; }
    [flow] F1_C1 = { TX > RX; }
}


[prop] = {
    [addresses] = {
        EX.Minus                                = (,)
        EX.Plus                                = (,)
        EX.TX                                  = (, )
        EX.RX                                  = (, )
    }
    [layouts file=""] = {   // System.{Interface 명} = (x, y, w, h)
        A."+" = (1309,405,205,83)
    }
}
