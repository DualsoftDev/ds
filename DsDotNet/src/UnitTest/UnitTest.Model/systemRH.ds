[sys] RH = {
    [interfaces] = {
        RUN = { F.Work ~ F.Done };
        ADV = { F.Vp ~ F.Sp };
        RET = { F.Vm ~ F.Sm };
        ADV <||> RET;
    }
    
    [flow] F = {
        Work > Done;
        Work = {
            Ap > Am;
        }
        Vp > Sp;
        Vm > Sm;
    }

    [jobs] = {
        Ap = { LH.ADV(%I1, %Q1); }
        Am = { LH.RET(%I2, %Q2); }
    }
  
    [external file="systemLH.ds" ip="localhost"] LH;
}
